<% layout('layouts/boilerplate')%>

    <style>
        .navbar{
            display:none;
        }
        .user-info{
            position:fixed;
            bottom: 0;
            background-color: #fffcf8;
            border-top: #ddd solid 1px;
            padding: 10px;
        }
        .dse00btn{
            font-size: 14px;
            padding:2px 5px;
            margin-top: 6px;
        }
    </style>
    <div class="inner-container">


        <div class='index-loop'>
            <div id="userbar">
                <% if(!currentUser) {%>
                    <b><a href="/login" target="_blank">
                        <span style="margin:0 10px">DSEJJ</span>｜<span id="level" style="margin:0 10px">Lv.1</span>｜ <span> 🪙 × 0 </span></a></b>

                    <% } %>
                        <% if(currentUser) {%>
                            <b>
                        <span>
                            <a href="/user/<%= currentUser._id %> ">
                                <%= currentUser.username.toUpperCase() %>
                            </a>
                        </span>｜
                        <span id="level">Lv.
                            <% if(currentUser) {%>
                                <%= currentUser.grade %>
                                    <% } %>
                        </span>｜
                        <a href="/dse00coin">
                            <span>🪙×
                                <% if(currentUser) {%>
                                    <%= currentUser.coin %>
                                        <% } %>
                            </span>
                        </a></b>
                            <% } %>
            </div><br>
            <a class="btn dse00btn" id="newpost" href="/campgrounds/new">新 POST</a>
            <% for (let i=0 ; i<campgrounds.length ; i++){%>
                <div class="card mb-2">
                    <div>
                        <a href="/campgrounds/<%=campgrounds[i]._id%>">
                            <div>
                                <div class="card-body" style="padding-bottom: 10px;">
                                    <h6 class="post-title">
                                        <%= campgrounds[i].title %> <small class="text-muted" style="font-size:14px; font-weight:200"> - <%=
                                                    campgrounds[i].author.username%></small>
                                    </h6>
                                    <!-- <span style="font-size:14px; font-weight:300; color:#999"><%= campgrounds[i].description.substring(0,40) %> </span> -->
                                    <span style="position: absolute; top:5px ; right:10px; color:#bbb">
                                        <% const r=campgrounds[i].reviews.length-1 %>
                                            <%= r+1 %> 💬
                                    </span>
                                    <% if (campgrounds[i].reviews[0]) { %>
                                        <span style="font-size:14px; color:#bbb" class="night-text"> ➥ <%=
                                                campgrounds[i].reviews[r].body %></span>
                                        <% } %>

                                </div>
                            </div>
                        </a>
                    </div>
                </div>
                <% }%>
                    <a class="btn dse00btn" href="/campgrounds/new" target="_blank">新 POST</a>
        </div>

        <div class="user-info">
            <div>
                <form action="/campgrounds/iframe" method="POST" novalidate class="validated-form" enctype="multipart/form-data">
                    <div>
                        <label class="form-label" for="title"><b>QUICK TALK</b></label>
                        <input style="height:60px;" class="form-control" type="text" id="title" name="campground[title]" required>
                        <div class="valid-feedback">
                            Looks good!
                        </div>
                        <button class="btn dse00btn" id="sendbtn">送出！</button>
                    </div>
                    <div style="opacity: 0; position: absolute; top:0">
                        <textarea type="text" style="height:25px;color:#bbb" id="description" name="campground[description]">如題</textarea>
                    </div>

                </form>
            </div>
        </div>




    </div>